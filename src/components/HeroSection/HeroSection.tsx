import styles from "./HeroSection.module.css";

const HeroSection = () => {
  return (
    <section>
      <div className={styles.container}>
        <h1>Discover</h1>
      </div>
    </section>
  );
};

export default HeroSection;
